# dependencies
/node_modules

# next.js / build output
/.next
/out

# velite generated content
/.velite

# typescript incremental build cache
*.tsbuildinfo

# env
.env
.env*.local

# secrets — GitHub App private keys, never commit
*.pem

# logs
*.log
npm-debug.log*

# os / editor
.DS_Store
.vscode
.idea

# playwright scratch
/.playwright-mcp

# claude code scratch
.claude/

# editable source-of-record design assets (151MB) — kept on disk, not tracked.
# See CLEANUP_REVIEW.md §7. Remove this line to version-control them.
/assets
public/feedback/
# image-generation scratch (source PNGs/MOVs, converted to webp under public/images)
new-image-generated*/
new_image_*/
new-images/
/pop/
_frames_tmp/
asset-prompts/
# avatar generation intermediates (53MB of 1024px PNGs + pilot/contact sheets).
# Shipped output is public/images/team/*.webp — regenerate via scripts/generate-brand-avatars.mjs.
public/images/team-v2/
# hero image staging — the generator itself labels this "스테이징 (검수용)" (15MB PNGs
# + pilot sheets). Only the reviewed webp output belongs in the repo.
public/images/hero-v2/
# scratch holding pen for things on their way out (source tarballs etc.)
/_to_delete/
# team avatar source material — real staff photos, kept local (privacy), not in repo
/person-raw/
public/images/team/_to_delete/
# internal business/pricing strategy doc — kept local, not in repo
docs/BIZ_SERVICE_PLAN_v1.md
# session planning / evaluation reference docs (dropped in root, kept local)
/DeepingSource_*.md
# local visual-verification screenshots
/*.jpeg

# generated copy review snapshot (regenerate: npm run copy:review)
copy-review/
content/_import/

# local-only working artifacts — source material + working folders, kept on disk, out of repo.
# See docs/REMAINING_WORK.md §E. (WIP plan/spec docs were archived into the repo 2026-07-20.)
/*.pdf
storeagent-demo-prep/
