# 통합 작업지시서 v1 — 진단(v3/v4) · 목업 시스템 · About 검수 (2026-07-23)

> **실행 주체: Claude Code 세션.** 본 문서는 실행 순서·커밋 단위·검증·리뷰 게이트만 정의한다. 상세 스펙은 전사하지 않는다 — **각 Stage 착수 시 인용된 원본 섹션을 반드시 직접 읽고 따를 것.**
> 원본 4종 (약칭):
> - **v3** = `docs/diagnosis-v3-plan.md` — 진단 6문항 확장 + 페이싱
> - **v4** = `docs/diagnosis-v4-engine-plan.md` — YAML 지식 베이스 + 적응형 엔진
> - **MM** = `docs/MOCKUP_MASTER_PLAN_v1.md` — 목업 시스템 단일 기준(결정 D1~D12)
> - **AB** = `copy-review/About_검수_개선계획_v1_260723.md` — About 검수·개선
>
> 우선순위 원칙(확정): **버그·즉효 먼저** → 기반(계약) 구축 → 구조 작업 순차. 진단 트랙(Stage 3·4·8)과 목업 트랙(Stage 5·7)은 상호 독립 — 병렬 세션 가능.

---

## 확정 결정 (Jamin, 2026-07-23) — J1~J8

작성 시점에 열려 있던 결정을 확정했다. 아래 값이 각 원본 문서의 해당 항목보다 우선한다.

| # | 결정 | 내용 | 반영 위치 |
|---|---|---|---|
| J1 | **Vision 2031 = 텍스트 3줄 축소** | 다크 목업 제거(D7 자연 해소), 목표는 '목표' 라벨 명시 (AB B3·D7) | Stage 6-2 |
| J2 | **E3(적응형 엔진)까지 확정** | 시나리오 확장 전제로 E3 진행 확정. 단 배포는 G8 지표 판단 유지 — 미달 시 fixed 종결 (v4 §4·§6) | Stage 8 |
| J3 | **D2 대응 = 기본안 먼저** | 숫자+워드마크+blue 강조로 1c 프로토타입 → 구분력 부족 시 B안(SAAI 데이터 hue 단계 칩) 투입 (MM §7) | Stage 5 G5 |
| J4 | **AB §2 카피 수정안 = 지금 확정** | 수정안 그대로 반영. G6는 사전 확정 게이트에서 **반영 후 diff 리뷰**로 경량화 | Stage 6-3 |
| J5 | **히어로 배경 = 라벨 없는 컷 교체** | 추적 ID 박스 없는 이미지로 교체 (AB D5) | Stage 6 |
| J6 | **VPE bio = 삭제 + focus 통합** | 경력 확보 대기 없이 진행 (AB Beat 5) | Stage 6-3 |
| J7 | **연혁 라운드 표기 = IR 페이지 기준** | 구현 세션이 InvestorsView·company.yaml 대조 후 타임라인·카드 통일 (AB 타임라인 03) | Stage 6-1 |
| J8 | **G4 퇴로 = Q3 강등 사전 승인** | 완료율 −5%p 초과 하락 확인 시 재논의 없이 Q3(규모)를 결과 후 질문으로 강등 (v3 §9) | Stage 4 G4 |

---

## 0. 공통 규칙 (전 Stage 적용)

1. **준수사항 승계**: v3 §10 전체(One Blue·3로케일 동시·reduced-motion·LLM 위장 금지·정적 배포·수치 resultsNote) + v4 §8(엔진 순수 함수·랜덤성 금지·selector 단일 지점) + MM §2-A 가드레일(목업 내부 hex 금지) + AB §4-6(**'유지' 판정 문장 불가침** — H1, "카메라는 결국, 사람을 봅니다.", 타임라인 05, CEO bio, 각주).
2. **카피**: 신규·수정 카피는 ko/en/jp 3로케일 동시. 카피 단독 커밋 금지(v3 §10). placeholder 금지 — **Jamin 확정 전 신규 작문 반영 금지**(MM Phase 2 규칙). AB 섹션별 표의 "수정안"은 확정 초안으로 간주하되, 게이트 G6에서 일괄 재확인.
3. **커밋 규칙**:
   - 1커밋 = 1관심사. 각 Stage에 명시된 커밋 단위 준수.
   - 커밋 전 필수: 타입체크 + lint + `npm run build`(콘텐츠 변경 시 gen 스크립트 포함) 통과. 실패 상태 커밋 금지.
   - 메시지 형식: `[diagnosis|mockup|about] 요약 (근거: 문서 §)` — 예: `[about] fix CountUp zero-flash (AB §1 A2)`.
   - 트랙별 브랜치 권장: `fix/quick-wins` · `feat/diagnosis-e1` · `feat/mockup-v2` · `feat/about-restructure`.
4. **검증·리뷰**: 각 Stage 말미 "검증" 통과 후, **G 게이트는 Jamin 승인을 받고** 다음 Stage 진행. 검증 결과(스크린샷·리포트·실측치)는 커밋 메시지 또는 PR 설명에 기록.
5. **착수 전 사전 확인** (Stage 1 이전, 1회):
   - MM §4-0: **Vercel 배포 SHA ↔ HEAD 대조** — A7·A10 재판단과 육안 검수 재개의 선행 조건.
   - 워킹트리 클린 확인, 현재 빌드 그린 확인(기준선 확보).

---

## Stage 1 — 버그·즉효 수정 (독립 배포, 최우선)

각 원본이 "즉시"로 지정한 항목만 모은다. 구조 변경 금지.

| 커밋 | 내용 | 근거 |
|---|---|---|
| 1-1 | About 렌더 버그: A1(스크롤 애니메이션이 콘텐츠 은폐) + A2(CountUp "0" 노출) + A4(히어로 H1 3줄 계단) — 각 항목의 "→" 수정 지침 그대로 | AB §1-A, §4 순서 1 |
| 1-2 | About 정합: A3 유령 CMS 필드 정리(중복 필드 제거 + method를 yaml 소비로 역전. vision/mission **렌더 연결은 Stage 6으로 이연**) + A5 "네 개의 근거" 숫자 불일치 | AB §1-A A3·A5, §4 순서 2 "정합 커밋 1회" |
| 1-3 | 진단 페이싱·스크롤: `DiagnosisConversation.tsx`만 변경 — 상단 앵커(§6-1)·공개 타이밍(§6-2)·결과 상단 정렬+순차 공개(§6-3)·되감기(§6-4) | v3 §8 Phase 1 ("최우선 단독 배포", v4 §6 동일) |

**검증**
- About: 360/768/1280 3폭 육안(AB A4 지시) — 빈 섹션·"0" 스탯·H1 계단 재현 불가 확인.
- 진단: v3 §7 말미 "페이싱 공통 검수" (a)~(d)를 **현행 질문 구조** 그대로, 3로케일 × 페이지/모달에서 확인. reduced-motion 지연 0 확인.

**게이트 G1**: 프리뷰 배포 → Jamin 육안 확인 후 merge.

---

## Stage 2 — 목업 기반 계약 구축 (MM Phase 0)

MM §5 Phase 0 표(0-1~0-7)를 순서대로. 스펙은 MM §2-A~2-D.

| 커밋 | 내용 |
|---|---|
| 2-1 | 0-1 토큰 codegen + `mockup-tokens.gen.ts` + `--saai-*` 스코프 (MM §2-A) |
| 2-2 | 0-2 `MockupViewport` + 프레임 3종 내장화 (MM §2-B) |
| 2-3 | 0-3 `mockup-motion.ts` + `useCountUpGroup` 리팩터 — **단독 커밋**(MM 명시, §7 리스크 대응) |
| 2-4 | 0-4 canonical v2 재산정(D6: 217·203/10/4·1,353·`ownerChainStores`) — 파생 컴포넌트 자동 반영 확인 (MM §2-D) |
| 2-5 | 0-5 레퍼런스 마이그레이션 `ChatMockup`(D9 하단 고정 포함) |
| 2-6 | 0-6 `/demo` 검증 하네스(3폭 × 3로케일) |
| 2-7 | 0-7 가이드 v2 + `DESIGN.md` D1 예외 조항(MM §0 문안) + lint 가드 |

**검증**: MM Phase 0 Acceptance 전 항목 — ChatMockup 3폭×3로케일 줄바꿈 변화 0 · CLS 0 · 내부 hex/spring 0건 · gen 멱등(2회 실행 diff 0) · 파생 화면 전부 217 계열.

**게이트 G2**: 하네스 렌더 스크린샷 리뷰(특히 ChatMockup 모범 구현).

---

## Stage 3 — 진단 지식 베이스 + 시뮬레이터 (v4 E1·E2)

**v3 Phase 2는 폐기** — 하드코딩 확장 금지, v4 E1으로 대체(v4 §6). 질문 트리·카피의 *내용*은 v3 §3(6문항 설계)·§4(연결 멘트)를 YAML로 승계한다.

| 커밋 | 내용 | 근거 |
|---|---|---|
| 3-1 | 스키마 + `gen-site-content.mjs` diagnosis 섹션 + 빌드 타임 검증 5종 | v4 §2-2·2-3·2-5 |
| 3-2 | `content/solutions/*.yaml`에 `diagnosis` 블록 태깅(21개 전부) + CLUSTER_MAP 파생 교체 | v4 §2-1 |
| 3-3 | 질문 뱅크(~15-18문항: v3 §3 신규 6 + 기존 tiebreak 6 + Q1~Q3·Q6) + `flow.yaml`(selector: fixed) + 연결 멘트(v3 §4 → ack 필드) — 3로케일 | v4 §2-2·2-3, v3 §3~4 |
| 3-4 | 엔진 인터프리터: `src/lib/diagnosis-engine/` React 무의존 순수 함수, weight는 전부 1로 시작(v4 §7) + Keystatic 컬렉션 | v4 §2-5·§8 |
| 3-5 | E2: `scripts/diagnosis-sim.mjs`(전 경로 열거) + fixtures(v3 §7 시나리오 A~E → v4 §2-4 형식) + vitest CI | v4 §5 |

**검증**
- 시뮬레이터 리포트 클린: 도달 불가 slug 0 · 경로 길이 min~max 위반 0 · dead copy 0.
- 골든 패스 5종(A~E) 통과. 빌드 검증 5종이 실제로 실패를 잡는지 네거티브 테스트 1회(고아 evidence 주입 → 빌드 중단 확인).
- 수동 검수: v3 §7 시나리오 A·D·E를 3로케일 × 페이지/모달에서 재연 — 사용자 체감 변화 없음(E1 목표) 확인.

**게이트 G3**: 시뮬레이터 리포트 + fixture 대본 3로케일 낭독 검수(v4 §7) — Jamin 확인.

---

## Stage 4 — 진단 신호 반영 + 계측 (v3 Phase 3·4)

E1 데이터 스키마 위에서 구현한다(v4 §6).

| 커밋 | 내용 | 근거 |
|---|---|---|
| 4-1 | `routing.ts` goal/scale 가중 재정렬(카드 4장 컷 유지) + 가격 시뮬레이터 프리셋 쿼리 + `ResultPanel` symptom 되받기 슬롯 | v3 §5 |
| 4-2 | umami: `diagnosis_step` 6스텝 반영 + `diagnosis_signal(goal, scale)` | v3 §8 Phase 4 |

**검증**: v3 §7 시나리오 A~E **전체** 재연 — 각 시나리오에 명시된 결과 slug·허브 카드 순위·CTA 처리·되받기 문장·진행바 단조 증가를 기대값으로 대조. 되감기 시 신호 재구축 정합(시나리오 D).

**게이트 G4 (비동기)**: 배포 후 완료율 v2 대비 −5%p 이내(v3 §8) 확인. **미달 시 J8에 따라 재논의 없이 Q3 강등 구현**(v3 §9 퇴로 사전 승인). E3 진행 자체는 J2로 확정 — G4는 시점 조정 요인일 뿐.

---

## Stage 5 — 목업 실사용 마이그레이션 (MM Phase 1)

배치 순서(노출 빈도): **1a 홈 계열 → 1b 제품 페이지 → 1c 솔루션·엔터프라이즈 → 1d 기술 다크 3종** (MM §5 Phase 1). 배치당 1커밋, 확정 수정 1-①~⑩은 해당 컴포넌트가 속한 배치 커밋에 포함.

- "재현 후" 조건 항목(1-⑧ A3 WordRise, 1-⑨ A5 스크롤)은 재현 시도 → 실패 시 스킵하고 사유 기록.
- 1c에서 미등록 4종 registry 등록 + `LiveDemoSection`·`AnonymizationMockup`(D10) `_archive` 이관(1-⑩).

**검증**: MM Phase 1 Acceptance — 실사용 14뷰+홈 emerald/violet 0건 · 호출부 사이즈 지정 "폭"만 · 하네스 그린 · **"200/240/128" 잔존 전수 grep 0건**(MM §7).

**게이트 G5**: 1c 완료 시점에 D2 프로토타입 확인. **J3 확정 — 기본안(숫자+워드마크+blue) 먼저, 구분력 부족으로 판단될 때만 B안(단계 칩) 투입.** Jamin에게는 프로토타입 스크린샷만 공유(판단 기준 이미 확정).

---

## Stage 6 — About 구조 재편 + 카피 (AB §3·§4)

AB §4 반영 순서 3~5를 커밋 3개로. 재구성 뼈대는 AB §3 표(사실 → 사람 → 방법 → 주장).

| 커밋 | 내용 | 근거 |
|---|---|---|
| 6-1 (구조) | B1 히어로 해체(사실 스트립 교체, 1뷰포트) + **히어로 배경 라벨 없는 컷 교체(J5)** + B2 연혁 통합(**라운드 표기 IR 페이지 기준 — J7**: InvestorsView·company.yaml 대조 후 통일) + D2 | AB §1-B, §4 순서 3 |
| 6-2 (재배열) | B3 섹션 정리(**Vision 2031 텍스트 3줄 축소 — J1**, 다크 목업 제거로 D7 해소·6B 한 줄화) + B4 신뢰 증거 보강(로고 밴드·스탯 재구성) + **vision/mission 실제 렌더(A3 완결)** + D6 프리미티브 마이그레이션 | AB §1-B, §3, §4 순서 4 |
| 6-3 (카피) | B5 텍스트 다이어트(2층 구조·40% 감축) + C1~C5 정책 + 섹션별 표 수정안 일괄(**J4로 확정 — 그대로 반영**) + **VPE bio 삭제·focus 통합(J6)** — 3로케일 | AB §1-C, §2, §4 순서 5 |

**게이트 G6 (경량화 — J4)**: 사전 확정 불필요. 6-3 반영 후 **카피 diff를 Jamin이 리뷰**하는 방식으로 대체. 홈 검수표 G 정책과 충돌 시 **홈 검수표 우선**(AB 말미).

**검증**: 본문 글자 수 40% 감축 실측(AB B5 기준: 헤딩·스탯·라벨 제외) · 3폭 육안 · '유지' 판정 문장 diff 0 · 스캔 레이어만 읽어도 서사 완결 여부 낭독 확인.

---

## Stage 7 — 목업 콘텐츠 핍진성 (MM Phase 2)

2-①~⑥ 순서대로, 항목당 1커밋(2-① 3로케일 동시 필수 — D7).

**게이트 G7 (착수 시)**: 신규 카피 발생분(2-③ 중립 매장명 등) Jamin 확정 — placeholder 금지.

**검증**: 핍진성 체크리스트 8항(MM §2-D)을 전 목업에 대해 통과 기록 · 표준 패턴(storeinsight/enterprise/simulator) 대비 파생 방식 일치 확인 · 하드코딩 수치 잔여 grep 0건.

---

## Stage 8 — 진단 적응형 엔진 (v4 E3·E4)

**진행 확정(J2)** — 선행 조건: Stage 3·4 완료(E1·E2 없이 직행 금지, v4 §4). G4 지표는 착수 시점 참고용이며 진행 여부를 바꾸지 않는다. 배포 여부만 G8에서 판단.

| 커밋 | 내용 | 근거 |
|---|---|---|
| 8-1 | 점수 모델(가중합) + usefulness 질문 선택 + dev 점수표 콘솔 | v4 §3-1·3-2 |
| 8-2 | 확인 스텝(템플릿+태그 조합) + reject 루프(maxRejects) + 결과 top2 병기 | v4 §3-3 |
| 8-3 | 정직성 가드레일·진행바 + E4 계측(질문별 분별 기여도·확인 부정률) | v4 §3-4·§6 |

전환은 `flow.yaml selector` 한 줄로만(v4 §2-3). fixed와 A/B.

**검증**: E2 셀렉터 동등성 리포트(fixed vs adaptive, v4 §5) + adaptive 전 경로 열거에서 확인 부정 루프 종료 보장 + fixture 대본 3로케일 낭독 검수(v4 §7).

**게이트 G8 (배포 판단)**: v4 §6 기준 — fixed 대비 완료율 동등 이상 · 평균 질문 수 감소 · 확인 부정률 20% 미만. 미달 시 E1(fixed) 유지로 종결 — 그 자체로 성립(v4 §0).

---

## Stage 9 — 마무리 (MM Phase 3·4 + 문서 정리)

1. 고아 재배치: MM §6 갭 맵 확정안(D12)대로 배치, 재배치분만 v2 마이그레이션 적용. registry 최종 정리(등록 = 실사용).
2. (옵션) Playwright /demo 스크린샷 diff CI + `lint:tokens` (MM Phase 4).
3. 문서 지위: MM §8 닫힘 고지 확인 · v3/v4/AB 문서 상단에 "본 지시서로 실행" 표기 · 본 지시서에 Stage별 완료·게이트 통과 기록.

---

## 부록 A — 게이트 요약

| 게이트 | 시점 | 승인 내용 |
|---|---|---|
| G1 | Stage 1 후 | 버그 수정 프리뷰 육안 |
| G2 | Stage 2 후 | 하네스·ChatMockup 모범 구현 |
| G3 | Stage 3 후 | 시뮬레이터 리포트 + fixture 대본 낭독 |
| G4 | Stage 4 배포 후 | 완료율 −5%p 이내 (비동기) — 미달 시 J8 자동 적용 |
| G5 | Stage 5 1c 중 | D2 프로토타입 스크린샷 공유 (판단 기준 J3 확정) |
| G6 | Stage 6-3 후 | About 카피 diff 리뷰 (J4로 경량화) |
| G7 | Stage 7 착수 시 | 목업 신규 카피 확정 |
| G8 | Stage 8 후 | adaptive 전환 지표 판단 |

## 부록 B — 병렬화 지도

```
Stage 1 (전 트랙 공통, 최우선)
  ├─ 진단 트랙: Stage 3 → 4 → (G4) → 8
  └─ 목업 트랙: Stage 2 → 5 → 7 → 9
About 트랙: Stage 6 — Stage 1 이후 언제든 착수 가능 (단 G6 카피 확정 필요)
```

교차 의존 없음. 단 세 트랙이 같은 파일을 건드리는 경우(예: `globals.css`, i18n)는 커밋 단위를 좁게 유지해 충돌 최소화.


---

## 부록 C — 실행 기록 (2026-07-23~24 구현 세션)

| Stage | 커밋 | 상태 |
|---|---|---|
| 1 버그·즉효 | `373d7da44`·`447f4593c`·`18eedef8c` | 완료 (G1: 사용자 진행 지시로 갈음) |
| 2 목업 기반 계약 | `877108642`~`bade5a852` (8커밋) | 완료 (G2: /demo/harness 비동기 리뷰 대기) |
| 3 진단 지식 베이스 E1·E2 | `7db4fe68f`~`7143943a9` (5커밋) | 완료 (G3: `npm run sim:diagnosis -- --script` 낭독 대기) |
| 4 진단 신호 반영+계측 | `c0449c784`·`7f45463fc` | 완료 (G4: 배포 후 완료율 지표 비동기 — 미달 시 J8 자동) |
| 5 목업 마이그레이션 1a~1d | `cfd407468`·`231bd67f8`·`9e7e3d6fb`·`8b2f8c8b5` | 완료 (G5: 기본안 채택 — 스크린샷 공유, B안 불요 판정) |
| 6 About 재편 | `28052e6a2`·`a1878d856`·`370f440e0` | 완료 (G6: 카피 diff 리뷰 대기 — `git show 370f440e0`) |
| 7 목업 핍진성 2-①~⑥ | `06a1cbf61`~`6661352c1` (6커밋) | 완료 (G7: 착수 시 확정 — 승인분 재사용·지명+점 규칙) |
| 8 적응형 엔진 E3·E4 | `34233317d`·`23fe0f0f4`·`e98348e60` | 완료 (G8: 배포 판단 대기 — selector=fixed 유지, 동등성 5/5·평균 6.0→4.2문항) |
| 9 고아 재배치+문서 정리 | 본 커밋 | 완료 (Phase 4 자동화는 옵션 — 미착수 기록) |

- 확정 수정 1-①~⑩ 전부 처리(⑧은 재현 실패로 스킵 — 근거: 클립=라인 박스 실측, ⑨는 재현 후 수정).
- PrivacyJourneyMockup: 갭 맵대로 후순위 보류 — registry 제외, 파일·/demo 갤러리 유지.
- 문서 지위: MM §8 닫힘 4종 고지 확인, v3/v4/AB 실행 표기(본 부록 참조 스탬프).
