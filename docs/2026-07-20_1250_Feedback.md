# 2026-07-20 12:50 피드백 반영 계획서

> 대상: Claude Opus / Claude Code 개발 세션. 로컬 HEAD `ceb2f8c1e` 기준.
> 소스: Jamin 피드백 10건(7/20 12:50) + `docs/GROWTH_PLANNER_STATUS_REVIEW_260720.md`(진행중 항목 코드 대조) 통합.
> 규칙: 카피 신규 창작은 Jamin 확정 후 반영(placeholder 금지). yaml 수정 시 `npm run gen:content` 재생성 필수. 신규 카피는 KO/EN/JP 동기화(2-5). 커밋 단위는 항목별 분리.

---

## 0. 선행: 이전 세션에서 이관된 긴급/대기 항목

| ID | 내용 | 상태 | 착수 조건 |
|---|---|---|---|
| **3-10** | 올리브영 실명 노출 제거 (런칭 블로커, 마감 7/21) | 위치 6곳 확정 | **즉시** — §0.1 |
| **3-11** | "누구가" 오탈자 — 문맥별 "누구나"/"누가" 교체 | 7곳 특정됨 | Jamin 단어 확정 대기 |
| **3-12** | 홈 하단 CTA "우리 매장 진단받기" 문구 교체 | `HomeView.tsx:46` 1곳 | Jamin 문구 확정 대기 (권장: "전문가 상담 신청하기") |
| **2-7** | 블로그 목록 export (C3 큐레이션용) | 스크립트만 작성하면 됨 | **즉시** — §0.2 |
| 2-1 | 아웃라이어 워크플로 섹션 | 스펙 `SPECS_NEXT_v1.md` §A1 | C1 카피 확정(7/23) 후 |
| 2-3 | store insight PRD 풀매핑 | — | **완료 처리됨(7/20 Jamin), 작업 불필요** |

### 0.1 [3-10] 올리브영 실명 제거 — 즉시
- `src/components/corporate/PartnerGrid.tsx:14-15` — ko/en 배열 첫 항목 `'올리브영'`/`'Olive Young'`
- `content/site/contact.yaml:22-23, 34-35, 78-79` — partnerText·mobileTrustBrands·brandPlaceholder
- 조치: 준실명 표기(예: "국내 1위 H&B 스토어") 또는 목록 제거. yaml 수정 후 `npm run gen:content`. PL-5(승인 후 실명 복원)를 위해 되돌리기 쉬운 방식으로.
- 다른 7개 브랜드(BGF CU·코리아세븐·CJ푸드빌·롯데GRS·현대차·롯데월드·국립박물관재단)의 승인 여부도 Jamin에게 확인 요청 — 미확인 시 일단 유지.

### 0.2 [2-7] 블로그 목록 export — 즉시
- `scripts/export-blog-list.mjs` 신규: `content/articles/*.mdx`(247개) frontmatter(title/slug/category/date/lang/draft) → `docs/BLOG_CURATION_LIST.csv`.
- `src/lib/articles.ts`의 노출 규칙(weekly 제외 등)을 재사용해 "현재 노출 vs 전체" 컬럼 포함 — Jamin이 C3 선별에 바로 사용.

---

## F-1. GNB/회사 표기 DeepingSource → DEEPINGSOURCE (Glacial Indifference)

**참고**: 폰트 정확한 이름은 **Glacial** Indifference (피드백의 "Glacier"는 오기). 이미 로드되어 있음 — `public/fonts/GlacialIndifference-{Regular,Bold}.otf`, `globals.css`의 `--font-brand` 변수와 `.font-brand` 패턴. 헤더 로고(`Header.tsx:149`)는 이미 `font-brand` + `DEEPINGSOURCE` 적용 완료.

**남은 작업** — UI 노출면의 혼용 표기 정리 (`DeepingSource` 75곳 / 약 20파일):
1. **1차 (이번 작업)**: 사용자 눈에 보이는 워드마크성 표기를 `DEEPINGSOURCE`로 통일 + 워드마크로 쓰이는 곳은 `font-brand` 클래스 적용.
   - `Header.tsx:61` (회사 소개 메뉴 desc "DeepingSource Inc.")
   - `Footer.tsx:65-66` (en/jp 회사 설명문)
   - `BlogArticleView.tsx` CTA eyebrow "DeepingSource"
   - views: `EnterpriseView` · `StoreCareView` · `NewsView` · `SpatialAiView` · `GlossaryView` · `DocsView` · `OperatingLoopGraphic` · `MinisiteFooter` · `StoreAgentContent` · blog/case-studies/events 라우트 파일들
   - content: `leadership.yaml` · `technology.yaml` · `resources.yaml` · `news.yaml` (수정 후 gen:content)
2. **예외 (변경 금지)** — `brand-canon.ts:21-22`에 명문화된 규칙 유지:
   - JSON-LD `name`/`legalName` (법인명 구조화 데이터)
   - 어원 라인 "DeepingSource = Deep Learning + Source" (`brand-canon.ts:176-180`)
   - 문장 중간의 자연 산문(예: Footer 설명문)은 대문자화하면 EN/JP 가독성이 떨어질 수 있음 — **워드마크(단독 표기)만 대문자, 산문 속 표기는 Jamin 확인 후 결정**.
3. `brand-canon.ts` 상단 주석에 이번 결정("UI 워드마크 = DEEPINGSOURCE + font-brand") 추가해 SOT화.

**Acceptance**: 전 페이지에서 단독 노출되는 회사명이 DEEPINGSOURCE + Glacial Indifference로 렌더. JSON-LD·어원 라인은 기존 유지. 3로케일 빌드 그린.

---

## F-2. tech/anonymizer 영역 이미지 원본 확보 — 자산 의존 (피트님 요청?)

**현황**: `AnonymizerView.tsx`는 두 개의 시각 자산을 사용 중.
- 슬라이더: `tech-anon-slider-before.webp`(**4.5KB — 저해상 의심**) / `-after.webp`(11KB)
- 데모 영상: `/videos/pete-anon-demo.{mp4,webm}` + `pete-anon-poster.webp` — 파일명상 "피트(Pete)" 관련 자산

**작업**:
1. **(Jamin, 운영)** 원본 고해상 소스 확보 — 피드백의 "피트님 요청?"이 이 데모 영상 원본을 피트님께 요청하는 건인지 확인 필요. 확인되면 요청 발송.
2. **(Claude, 수령 후)** 교체 스펙: 슬라이더 before/after는 동일 프레임·동일 크기(현재 비율 유지), webp 재인코딩(품질 80~85, 원본은 `assets/`에 보존), 영상은 mp4+webm 이중 인코딩 + 포스터 재추출.
3. 수령 전 코드 작업 없음 — **이 항목은 자산 도착까지 블로킹**.

---

## F-3. Vision Model 리스트업 + 데모 영상 + 문제 매핑

**현황**: `ModelsView.tsx` — 모델 카드 배열(`t.models`, 이미지 `/images/models/{name}.webp`), 각주에 "위 목록은 대표 모델을 설명하기 위한 예시"라고 명시된 상태. 개선계획 2-C에서도 "카탈로그 애니메이션·표현 애매" 지적.

**작업** (2단계):
1. **(Jamin/제품팀, 자료)**: 실제 공개 가능한 모델 리스트 확정 + 모델별 데모 영상 확보. 산출물 형식 제안 — 모델명 / 해결하는 문제(1줄) / 적용 제품(count·insight·care·agent 중) / 데모 영상 유무.
2. **(Claude, 코드)**: 자료 수령 후 —
   - `ModelCopy` 타입 확장: `problem`(해결하는 문제) · `products`(적용 제품 배지) · `video`(선택) 필드
   - 카드 UI 재설계: "모델 이름 나열"에서 "**이 문제를 → 이 모델이** " 구조로 전환 (개선계획 2-C의 시각화 요구와 통합)
   - 데모 영상 있는 모델은 hover/클릭 시 `LoopVideo` 재생 (기존 컴포넌트 재사용)
   - "예시" 각주는 실리스트 반영 후 제거
3. 선행 가능한 것: 타입·레이아웃 골격은 자료 없이도 준비 가능(현행 데이터로 렌더 유지). 단, **실제 모델명·문제 매핑 텍스트는 placeholder 금지** — 자료 도착 후 채움.

---

## F-4. solutions/ 제품별 콘텐츠 보강

**현황**: `RetailView`·`FoodBeverageView`·`DrugView`(→drug-store)·`LargeSpaceView` 4개 업종 페이지에 saai count/insight/care/agent **언급 자체가 0건**(grep 확인). 업종의 문제는 말하는데 "그래서 어떤 제품을 쓰는지"가 없음.

**작업**:
1. 공용 컴포넌트 `SolutionProductMap`(가칭) 신설 — 업종별 권장 제품 조합을 카드로 표시, 각 카드는 제품 페이지로 링크.
2. 업종→제품 매핑(초안, Jamin 확정 필요):
   - 리테일·편의점: count(유입) + insight(동선·매대) + agent(운영 제안)
   - 카페·음식점: count(피크·회전) + care(위생·이상)
   - 드럭스토어: insight(카테고리 체류) + agent(진열)
   - 대형 공간: insight(MTMC 동선) + care(안전·혼잡)
3. 매핑 카피는 `content/site/{retail,food-beverage,drug,large-space}.yaml`에 필드 추가(§3 레시피 준수: yaml → gen → View 소비). KO/EN/JP.
4. F-5(케이스 연결)와 같은 섹션 블록에 배치하면 한 번의 레이아웃 작업으로 처리 가능.

**Acceptance**: 4개 업종 페이지 각각에 제품 조합 섹션 렌더 + 제품 페이지 링크(localeHref, /ko·/jp 404 없음).

---

## F-5. solutions/ → /resources/case-studies 연결

**현황**: 솔루션 4개 뷰 + 허브에서 case-studies로의 링크 **0건**(grep 확인). 케이스는 `caseStudies` 컬렉션(15 mdx, 업종 아이콘 보유)으로 CMS화 완료 상태라 연결만 하면 됨.

**작업**:
1. `caseStudies` frontmatter의 업종 필드로 업종별 필터 getter 추가(`src/lib/case-studies.ts`).
2. 각 업종 페이지 하단(CTA 위)에 해당 업종 케이스 1~2건 카드 + "도입 사례 전체 보기 →" 링크. 기존 `CaseStudyCard` 재사용.
3. 해당 업종 케이스가 없으면 섹션 자체를 렌더하지 않음(빈 상태 노출 금지).
4. 허브(`SolutionsView`)에도 case-studies 진입점 1곳 추가.

---

## F-6. /company/about 회사 소개 업데이트 — 창업 스토리 반영

**소스** (둘 다 레포에 존재):
- `content/articles/the-birth-of-deeping-source-ko.mdx` (+ en판 `the-birth-of-deeping-source.mdx`)
- `content/articles/deeping-source-story-about-deeping-source-not-dipping-source-ko.mdx` (ko만 — en/jp 확인 필요)

**작업**:
1. 두 글에서 창업 서사·"Deeping(≠Dipping) Source" 이름 유래·미션 스토리를 발췌해 `content/site/about.yaml`의 `story` 섹션(74행~) 보강. 기존 어원 라인(`brand-canon.ts` etymology)과 표현 충돌 없도록 대조.
2. about 페이지 story 섹션에 두 블로그 글로의 "전체 이야기 읽기" 링크 추가(localeHref).
3. **주의**: 발췌·요약문 초안은 Claude가 작성하되 **Jamin 검수 후 반영**(C1 카피 검수 흐름에 편입). EN/JP는 원글 로케일판 있는 것만 링크, 없으면 KO 링크 + 링크 라벨만 로케일화.
4. 리더십 문장 잘림(개선계획 0-11)이 아직 미수정이면 이 작업에서 함께 처리.

---

## F-7. blog 다음 글 네비게이션 + 전 페이지 CTA

**현황 (실측)**:
- `BlogArticleView.tsx`(167줄): 본문 → 태그 → **CTA 섹션은 있음**. 그러나 **이전/다음 글 네비 없음, relatedSlugs(관련 글) 렌더도 없음** — frontmatter에 `relatedSlugs` 필드는 존재(Keystatic A-1에서 lint까지 구축)하는데 소비처가 없는 상태.
- docs에는 prev/next 자동 네비가 이미 구현되어 있음(STATUS §7 G19, section·order 기반) — 패턴 재사용 가능.

**작업**:
1. **관련 글**: `relatedSlugs` 기반 카드 2~3장(기존 `ArticleCard`/`BlogCard` 재사용). relatedSlugs 비어 있으면 같은 카테고리 최신글로 폴백.
2. **이전/다음 글**: 같은 로케일·발행일 순 prev/next 링크 바(도크 prev/next 크로스 금지 규칙처럼, weekly 등 비노출 세트와 섞이지 않게 `articles.ts` 노출 규칙 준수).
3. 배치: 태그 아래 → 관련 글 → prev/next → CTA 순.
4. **전 페이지 CTA 감사**: 29페이지 기준으로 CTA(Contact 연결) 없는 페이지 목록화 후 일괄 추가. 확인 우선순위 — glossary 상세, docs 상세, events, news, career, faq. 공용 `PageCta` 패턴이 이미 여러 뷰에 있으므로 재사용.

**Acceptance**: 블로그 상세 3로케일에서 관련 글+prev/next 렌더, 링크 로케일 정합. CTA 없는 페이지 0.

---

## F-8. resources/docs 제품별 매뉴얼 구분

**현황**: 이미 상당 부분 구현됨 — `DocsSidebar.tsx`가 "공통 안내 + 제품별 매뉴얼(접이식, 제품당 1그룹)" 구조, `ProductManualNav.tsx` 존재, saai count 매뉴얼 재구축 커밋(`163a1ef32`)·prev/next 제품 간 크로스 방지(`e0ec0a1ef`)까지 완료.

**작업 (갭 확인 후 보강)**:
1. **허브(`DocsView`) 점검**: 사이드바는 제품별 구분이 되어 있으나 허브 랜딩이 제품별 입구를 명확히 보여주는지 확인 — 제품 4종(count·insight·care·agent) 매뉴얼 카드 섹션이 없으면 추가.
2. 제품 매뉴얼 랜딩(store-insight/store-care/store agent/saai count) 4종이 모두 존재·일관된 구조인지 대조 — care는 22문서(jp 폴백), insight 96, agent 22페이지, count 신규. 빠진 제품 랜딩 있으면 신설.
3. URL 체계가 `<product>-<name>` slug 컨벤션으로 통일되어 있는지 감사(3-7 "Coming soon" 매뉴얼 2건 처리와 연계 — 이건 Jamin 방침 결정 대기).
4. **Jamin 확인 필요**: "구분하기"의 의도가 ① 허브에서 제품별 입구 강화(위 1~2)인지 ② URL 자체를 `/docs/<product>/...`로 재구조화(리다이렉트 수반, 큰 작업)인지. **①로 추정하고 진행하되 착수 전 확인.**

---

## F-9. /faq 디자인 전면 재검토 ("AI slop" 해소)

**현황**: `FaqView.tsx`(145줄) — 섹션별 `Accordion` 나열. 개선계획 2-B에서 이미 동일 지적: anonymizer·seal 페이지 FAQ 포함 "사이트 전체 FAQ 컴포넌트 통일 + design-system 기준 재설계".

**작업** (개선계획 2-B와 통합 실행):
1. `design-system/` 디렉토리의 기존 패턴을 기준으로 FAQ 컴포넌트 1종 재설계 — 타이포·간격·아이콘·모션을 사이트 톤과 정합.
2. 재설계 방향 후보(디자인 시안 1~2개를 Jamin에게 먼저 제시 후 확정):
   - 좌측 카테고리 스티키 네비 + 우측 질문 리스트(docs 사이드바 패턴 재사용)
   - 검색/필터 입력(개선계획 P3-4 리소스 검색과 연계 — 이번엔 FAQ 로컬 필터만)
   - 아코디언 유지하되 밀도·모션 조정(현재 "질문 나열" 인상 해소)
3. 통일 적용 대상: `/resources/faq` + `AnonymizerView`·`SealView` 인라인 FAQ + Spatial AI 페이지(FAQ 신설, 개선계획 2-B).
4. `FAQPage` JSON-LD는 이미 구현(`faqPage()`) — 리팩터 시 유지 필수.
5. FAQ 문체 통일(격식체/구어체 혼재, 분석 8-D)은 카피 작업이라 별도 — 디자인과 분리 커밋.

---

## 실행 순서 제안 (개발 세션용)

| 순서 | 항목 | 의존성 | 규모 |
|---|---|---|---|
| 1 | §0.1 [3-10] 올리브영 제거 | 없음 — **런칭 블로커, 최우선** | XS |
| 2 | §0.2 [2-7] 블로그 export | 없음 | XS |
| 3 | F-1 DEEPINGSOURCE 표기 | 산문 속 표기 범위만 Jamin 확인 | S |
| 4 | F-7 blog 네비+CTA 감사 | 없음 | M |
| 5 | F-5 solutions→case-studies | 없음 | S |
| 6 | F-4 solutions 제품 매핑 | 매핑·카피 Jamin 확정 | M |
| 7 | F-8 docs 구분 보강 | 의도 ①/② Jamin 확인 | S~L |
| 8 | F-6 about 스토리 | 발췌 초안 → Jamin 검수 | M |
| 9 | F-9 FAQ 재디자인 | 시안 → Jamin 확정 | L |
| 10 | F-3 모델 리스트/영상 | 자료·영상 확보(외부) | M |
| 11 | F-2 anonymizer 원본 | 자산 확보(외부) | XS(교체만) |
| — | 3-11·3-12 문구 | Jamin 단어/문구 확정 대기 | XS |

**Jamin 확인 대기 목록** (개발과 병렬로 회신 필요):
1. 3-11 — 7곳 문장별 "누구나" vs "누가" 확정
2. 3-12 — CTA 대체 문구 확정
3. F-1 — 산문 속 "DeepingSource"(Footer 설명문 등)도 대문자화할지
4. F-2 — "피트님 요청"이 pete-anon 데모 영상 원본 요청 건인지
5. F-4 — 업종→제품 매핑 초안 승인
6. F-8 — docs 구분의 의도(허브 강화 vs URL 재구조화)
7. 나머지 파트너 7개 브랜드 실명 사용 승인 여부
