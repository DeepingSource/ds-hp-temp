# ds-hp-temp — DeepingSource homepage (static preview)

Static export of the DeepingSource marketing site for GitHub Pages (private/org-only).
NOTE: static design preview — contact form/API, next.config redirects, and the
minisite middleware are inactive here. Use the real deployment for those.
